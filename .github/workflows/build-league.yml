name: Build SMP league site

on:
  push:
    branches: [main]
    paths:
      - "league-data/*.json"
      - "scripts/league_generator.py"
      - "scripts/projections.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-league
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python3 -m pip install --upgrade numpy

      - name: Generate league site from the newest export
        run: |
          # Pick the export with the highest (season, phase). Robust across the
          # day*/playoff*/preseason naming and season rollovers, unlike a filename glob.
          NEWEST=$(python3 - <<'PY'
          import glob, json
          best = None
          for p in glob.glob("league-data/*.json"):
              try:
                  ga = json.load(open(p)).get("gameAttributes", {})
              except Exception:
                  continue
              if isinstance(ga, list):
                  ga = {x["key"]: x["value"] for x in ga if isinstance(x, dict) and "key" in x}
              season, phase = ga.get("season"), ga.get("phase")
              if not isinstance(season, int):
                  continue
              key = (season, phase if isinstance(phase, int) else -1, p)
              if best is None or key > best[0]:
                  best = (key, p)
          print(best[1] if best else "")
          PY
          )
          if [ -z "$NEWEST" ]; then echo "No usable export found"; exit 1; fi
          echo "Building from $NEWEST"
          rm -rf assets games players teams
          rm -f compare.html draft.html free-agency.html history.html index.html projections.html records.html schedule.html trade.html
          python3 scripts/league_generator.py "$NEWEST" --out .

      - name: Commit regenerated site
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets games players teams compare.html draft.html free-agency.html history.html index.html projections.html records.html schedule.html trade.html
          if git diff --cached --quiet; then
            echo "No league changes."
          else
            git commit -m "Regenerate SMP league site [skip ci]"
            git push
          fi
